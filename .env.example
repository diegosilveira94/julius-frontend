# URL base da API.
# Em desenvolvimento, "/api" usa o proxy do Vite (vite.config.js) para http://localhost:3000.
# Para apontar direto para o backend, use por exemplo: http://localhost:3000/api
VITE_API_URL=/api
